#include <iostream>
#include <vector>

using namespace std;

vector<int> subsets(vector<int>& nums, vector<int>& subset, vector<vector<int>>& result) {

}

vector<vector<int>> subsets(vector<int>& nums) {

}

int main()
{
    vector<int> nums {1, 6, -1, 3, 2, 7, 11};

    return 0;
}
